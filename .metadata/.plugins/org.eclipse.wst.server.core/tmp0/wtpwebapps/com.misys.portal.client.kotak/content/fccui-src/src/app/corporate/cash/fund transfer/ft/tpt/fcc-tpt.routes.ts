import { NgModule } from '@angular/core';
import { Routes, RouterModule } from '@angular/router';

const fccTptRoutes: Routes = [
];

@NgModule({
 imports: [RouterModule.forChild(fccTptRoutes)],
 exports: [RouterModule ]
})
export class FccTptRoutes { }
